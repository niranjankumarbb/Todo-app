{
  "name": "project",
  "version": "1.0.0",
  "description": "connecting fe be db",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run  install-client && npm run build",
    "start": "node index.js",
    "client": "cd client && npm start"
 }, 
"engines": {
    "node": "^10.3.0", 
    "npm": "^6.8.0" 
  },
  "author": "niranjan",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2",
    "bcryptjs": "^2.4.3",
    "bootstrap": "^4.5.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "moment": "^2.26.0",
    "mongoose": "^5.9.11",
    "validator": "^13.0.0"
  }
}
